services:
  app:
    build:
      context: .
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./:/app
      - /app/node_modules
    restart: unless-stopped
